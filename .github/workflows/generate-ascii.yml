name: Generate ASCII Artwork
on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v4  #to read code from repo

        -name: List Repo files
          run: ls-ltra

        - name: Executing Shell Script
          run: ./ascii-script.sh